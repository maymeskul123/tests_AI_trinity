# Выводы по test27

- AdaptiveThreshold **не превзошёл** фиксированный порог (test25).  
- При `conflict_rate = 0.4`, `budget = 10`:  
  - `false_confidence = 0.339` (против 0.102 у Threshold)  
  - `reward = –34.39` (против –10.86 у Threshold)  
- Оценка шума через окно оказалась неточной, порог не обеспечил нужного баланса.  
- **Вывод:** фиксированный порог (3) остаётся лучшим.
